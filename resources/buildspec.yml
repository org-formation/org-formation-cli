version: 0.2
phases:
  install:
    commands:
      - npm install aws-organization-formation -g
      - echo installed aws-organization-formation
      - org-formation -v
  build:
    commands:
      - org-formation perform-tasks ./organization-tasks.yml --no-colors XXX-ARGS
  post_build:
    commands:
      - echo Build completed
